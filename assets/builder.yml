apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: provisioner-builder-tmp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: provisioner-builder
spec:
  selector:
    matchLabels:
      app: provisioner-builder
  template:
    metadata:
      labels:
        app: provisioner-builder
    spec:
      containers:
        - name: provisioner-builder
          image: registry.gitlab.com/pojntfx/projects/provisioner-builder
          resources:
            limits:
              memory: 256Mi
              cpu: 250m
          volumeMounts:
            - name: provisioner-builder-tmp
              mountPath: /tmp/pojntfx/provisioner
          env:
            - name: TRANSPORTER
              value: redis://provisioner-redis-transporter:6379
      volumes:
        - name: provisioner-builder-tmp
          persistentVolumeClaim:
            claimName: provisioner-builder-tmp
